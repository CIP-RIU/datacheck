
atable = as.data.frame(cbind(c1 = character(300), c2=numeric(300), c3 = character(300)),
                       stringsAsFactors = FALSE)


data_fingerprint <- function(atable, colors = 
                               c("ligthgreen", "lightyellow", "lightblue", "black"),
                             page_start = 1, page_size = 500){
  
}